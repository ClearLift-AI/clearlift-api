# Python dependencies for R2 Data Lake Bulk Loader

# Core dependencies
pyiceberg[s3fs,pyarrow,pandas]==0.7.0
pandas>=2.0.0
pyarrow>=14.0.0
pyyaml>=6.0
tqdm>=4.65.0

# AWS S3 compatibility for R2
boto3>=1.28.0
s3fs>=2023.10.0

# Data processing
numpy>=1.24.0
python-dateutil>=2.8.2

# Optional: For advanced features
duckdb>=0.9.0  # For local testing/validation
fastparquet>=2023.10.0  # Alternative Parquet engine
rich>=13.0.0  # For better CLI output

# Development dependencies (optional)
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0