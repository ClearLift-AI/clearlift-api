# ClearLift API Environment Variables
# Copy this file to .env and fill in your values

# R2 Data Catalog Configuration
DATALAKE_CATALOG_URI=https://catalog.cloudflarestorage.com/your-account/your-catalog
DATALAKE_WAREHOUSE_NAME=your-warehouse-name
R2_S3_API_URL=https://your-account.r2.cloudflarestorage.com/your-bucket
R2_BUCKET=your-bucket-name

# These should be set as secrets in Cloudflare dashboard, not in .env
# 
# R2 API Tokens (Cloudflare API tokens with R2 permissions)
# Create these in Cloudflare Dashboard > R2 > Manage API tokens
# R2_READ_ONLY_TOKEN=<Cloudflare API token with Admin Read Only permissions>
# R2_WRITE_TOKEN=<Cloudflare API token with Admin Read & Write permissions>
#
# Note: R2_READ_ONLY_ACCESS_ID and R2_READ_ONLY_ACCESS_SECRET are generated
# from the API token but are typically not needed when using the token directly
#
# Debug access
# DEBUG_TOKEN=<set in Cloudflare secrets>