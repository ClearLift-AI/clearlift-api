# ClearLift API Worker Environment Variables
# Copy this file to .env.local and fill in your values

# Cloudflare Configuration
CLOUDFLARE_ACCOUNT_ID=your_account_id_here

# Database Configuration (D1)
DATABASE_ID=89bd84be-b517-4c72-ab61-422384319361

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
# Note: We use both Publishable and Secret keys for different operations

# R2 Configuration
R2_BUCKET_NAME=clearlift-db

# ===============================================
# SECRETS - Now managed via Cloudflare Secrets Store
# ===============================================
# All secrets are configured in wrangler.jsonc using Secrets Store bindings
# Store ID: b97bbcc69dce4f59b1043024f8a68f19
#
# Available secrets in Secrets Store:
# - ENCRYPTION_KEY: Master encryption key for field-level encryption
# - SUPABASE_SECRET_KEY: Backend API key for Supabase
# - SUPABASE_PUBLISHABLE_KEY: Public API key for Supabase
# - R2_ADMIN_TOKEN: API token for R2 SQL queries (mapped to R2_SQL_TOKEN)
# - GOOGLE_CLIENT_ID: Google OAuth client ID
# - GOOGLE_CLIENT_SECRET: Google OAuth client secret
# - GOOGLE_ADS_DEVELOPER_TOKEN: Google Ads API developer token
# - FACEBOOK_APP_ID: Facebook app ID for OAuth
# - FB_API_KEY: Facebook app secret (mapped to FACEBOOK_APP_SECRET)
#
# To manage secrets, use Cloudflare dashboard or wrangler CLI:
# npx wrangler secrets-store secret create <STORE_ID> --name <SECRET_NAME> --remote

# Worker Health Check URLs
CRON_WORKER_URL=https://clearlift-cron-worker.your-subdomain.workers.dev
QUEUE_WORKER_URL=https://clearlift-queue-consumer.your-subdomain.workers.dev

# Environment
NODE_ENV=production